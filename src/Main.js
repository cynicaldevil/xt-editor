import React, { Component } from 'react';
import TextEditor from './TextEditor';

class Main extends Component {
    render() {
        return (
            <TextEditor />
        );
    }
}

export default Main;
